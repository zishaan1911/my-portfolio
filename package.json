{
  "name": "zishaan1911.github.io",
  "private": true,
  "type": "module",
  "description": "Zishaan Ahmed's personal site: hand-written static HTML plus a bot that keeps its live sections current.",
  "scripts": {
    "serve": "python -m http.server 8000",
    "sync": "node scripts/sync.mjs",
    "test": "node scripts/guard.test.mjs && node scripts/render.test.mjs && node scripts/sync.test.mjs"
  },
  "engines": { "node": ">=20" }
}
